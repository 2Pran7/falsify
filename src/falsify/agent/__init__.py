"""Module 4: the agent loop.

session   handle store — pipeline outputs stay here, the model gets handles
tools     the four tool schemas, dispatch and validation
loop      the conversation loop and its cost guards
"""
